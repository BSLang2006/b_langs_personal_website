import { Component } from '@angular/core';
import { Entry } from '../../shared/entry/entry';

@Component({
  selector: 'app-kit',
  imports: [Entry],
  templateUrl: './kit.html',
})
export class Kit {}
