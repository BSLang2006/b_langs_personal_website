import { Component } from '@angular/core';
import { Entry } from '../../shared/entry/entry';

@Component({
  selector: 'app-ospf',
  imports: [Entry],
  templateUrl: './ospf.html',
})
export class Ospf {}
