import { Component } from '@angular/core';

// A diagram is a component with no logic: markup in the .html, styles in the
// .scss, nothing here. Drop <app-cml-networking /> into any post.
@Component({
  selector: 'app-cml-networking',
  templateUrl: './cml-networking.html',
  styleUrl: './cml-networking.scss',
})
export class CmlNetworking {}
